export enum UserOrIntentStatus {
  APPROVED = 'approved',
  PENDING = 'pending',
  RETRY = 'retry',
  DOCUMENT = 'document',
  SUSPENDED = 'suspended',
  REJECTED = 'rejected',
  CONVERTED_TO_USER = 'converted_to_user',
}
